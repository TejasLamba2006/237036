{
  "name": "logging_middleware",
  "version": "1.0.0",
  "private": true,
  "description": "This is the logging middleware package that other microservices can use to send logs to the external logging service.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "axios": "^1.8.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.8.0"
  }
}